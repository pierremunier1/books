dist: bionic
language: python

python:
  - '3.8'

addons:
 
  firefox: latest

install:
  - pip install pipenv
  - pipenv install --dev

before_script:
 
  - install-webdrivers


branch:
   only:
    - main
  

services:
  - postgresql

script:
  - python manage.py test read_books